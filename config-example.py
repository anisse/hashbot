#!/usr/bin/env python
#
# Sample config for hashbot
#
#

import sys
# Requests configuration dict
r_config = {
        # enable debug
        'verbose': sys.stderr,
        }

# Hashbot twitter OAuth params
oauth_credentials = {
        'access_token' : "",
        'access_token_secret': "",
        'consumer_key' : "",
        'consumer_secret' : "",
}

